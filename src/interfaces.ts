export type Byte = [
  string,
  string,
  string,
  string,
  string,
  string,
  string,
  string
];

export type QuestionMode = "decimal-to-binary" | "hex-to-binary";
export type PreviewMode = "hide" | "original" | "filter";
